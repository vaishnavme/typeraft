import ThemePalette from "./theme-palette";

const Footer = () => {
  return (
    <footer className="absolute w-full left-0 bottom-0 p-1">
      <ThemePalette />
    </footer>
  );
};

Footer.displayName = "Footer";

export default Footer;
